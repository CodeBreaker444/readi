meta {
  name: Update Planning
  type: http
  seq: 17
}

post {
  url: {{baseUrl}}/api/evaluation/planning/update-planning
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "planning_id": 1,
    "status": "APPROVED"
  }
}

settings {
  encodeUrl: true
}

docs {
  **Purpose:** Updates a planning mission's configuration, assignment, or status.

  **Auth:** Session cookie (`readi_auth_token`). Requires `view_planning` permission.

  **Used in:**
  - `src/components/planning/PlanningMissionContent.tsx` — save button in the planning mission form
}
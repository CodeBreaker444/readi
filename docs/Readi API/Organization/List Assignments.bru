meta {
  name: List Assignments
  type: http
  seq: 12
}

get {
  url: {{baseUrl}}/api/organization/assignment
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
}

docs {
  **Purpose:** Returns user-role assignments for the organization showing who is assigned to which role/position.

  **Auth:** Session cookie (`readi_auth_token`).

  **Used in:**
  - Organization management pages — role assignment displays
}
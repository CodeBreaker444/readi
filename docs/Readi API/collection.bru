meta {
  name: Readi API
}

auth {
  mode: none
}

vars:pre-request {
  baseUrl: http://localhost:3000
  apiKey: 
}

docs {
  Complete API collection for the Readi drone operations management platform. Base URL: {{baseUrl}} | Auth: Session cookie (readi_auth_token) is set automatically on login. For external API endpoints use X-API-Key header.
}

---
version: "1.0.3"
date: "2026-06-09"
title: "Component Expiry Types, Decommissioned System Awareness & Logbook Status Fix"
description: "Components can now expire by date, flight count, or both. The AI agent can now answer questions about decommissioned systems and expired components. The logbook system filter now shows the correct operational status for each drone."
status: "stable"
releaseType: "minor"
tags:
  - ai-agent
  - decommissioned
  - operation-logbook
  - system-status
  - components
  - expiry
  - follow-up-questions
highlightCards:
  - title: "Component Expiry Types"
    icon: "timer"
    color: "violet"
  - title: "Decommissioned AI Awareness"
    icon: "shield-alert"
    color: "orange"
  - title: "AI Follow-up Questions"
    icon: "rocket"
    color: "purple"
  - title: "Logbook System Filtering"
    icon: "filter"
    color: "blue"
---

## What's New

### Component Expiry Types

Components now support three expiry modes, selectable when adding or editing a component:

- **Expiration Date** — the existing behaviour. The component is automatically decommissioned on a specific calendar date.
- **Flights** — the component is decommissioned once its flight counter reaches the configured limit.
- **Mixed** — both an expiration date and a flight limit are set. The component is decommissioned as soon as either condition is met — whichever comes first.

When a component's expiry threshold is reached the system it belongs to is immediately reflected as non-operational across the Fleet and Operations pages, consistent with date-based expiry.

---

### AI Agent — Decommissioned System Awareness

The AI agent can now answer questions about decommissioned systems and expired components.

The agent will list every affected system along with the components that have expired, grouped clearly by drone. If everything is in order, it will confirm that no decommissioned systems were found.

---

### AI Agent — Follow-up Questions

After the AI answers a question, it now suggests up to 3 relevant follow-up questions. These appear as clickable chips below the response — tap any chip to pre-fill the input and continue the conversation without retyping.

---

### Operation Logbook — System Filter Now Shows Correct Status

Previously, all drones appeared as "Operational" in the logbook system filter regardless of their actual state. The filter now reflects each drone's real status, so non-operational or under-maintenance systems are correctly labelled when filtering logbook entries.

---

## Bug Fixes

### Logbook system filter showed wrong status for all drones

**Issue:** Every drone in the Operation Logbook filter appeared as "Operational" even when it was grounded for maintenance or had an expired component.

**Fix:** The filter now reads each drone's actual status from the platform, so what you see in the filter matches what is shown on the Fleet and Operations pages.
